import React from "react";
import { LoadingCircle, SpinnerContainer } from "./index";

function Loading() {
  return (
    <SpinnerContainer>
      <LoadingCircle />
    </SpinnerContainer>
  );
}

export default Loading;
